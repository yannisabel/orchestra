// Type augmentation to resolve TypeScript constraint errors
// The Stencil React Output Target generates code with type constraints that
// don't account for optional aria attributes, so we provide this override.

export {} from './orchestra-checkbox.ts';
