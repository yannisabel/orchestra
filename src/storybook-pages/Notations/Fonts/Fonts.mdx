import { Canvas, Meta, Story, Unstyled } from '@storybook/blocks'
import { Box } from '../../../Staves/Box'
import { Text } from '../../../Staves/Text'

import {fontFamilies, fontSizes, fontWeights} from '.'

<Meta title="Notations/Fonts" />

<Unstyled>
<Box className="dark">

# Fonts

## Font Family

<Box>
  {Object.entries(fontFamilies).map(([fontFamily, value]) => {
    return (
      <Box key={`${fontFamily}`}>
        <Text fontFamily={fontFamily}>
          fontFamily: {fontFamily}
        </Text>
        <Text fontFamily={fontFamily}>
          value: {value}
        </Text>
      </Box>
    )
  })}
</Box>

## Font Size

<Box>
  {Object.entries(fontFamilies).map(([fontFamily, value]) => {
    return (
      <Box key={`${fontFamily}`}>
        <Text fontFamily={fontFamily}>
          fontFamily: {fontFamily}
        </Text>
        <Text fontFamily={fontFamily}>
          value: {value}
        </Text>
        <Box>
          {Object.entries(fontSizes).map(([fontSize, value]) => {
            return (
              <Box key={`${fontSize}`} className="flex-direction--row">
                <Text fontFamily={fontFamily} fontSize={fontSize}>
                  fontSize: {fontSize}
                </Text>
                <Text fontFamily={fontFamily} fontSize={fontSize}>
                  value: {value}
                </Text>
              </Box>
            )
          })}
        </Box>
      </Box>
    )
  })}
</Box>

## Font Weight

<Box>
  {Object.entries(fontFamilies).map(([fontFamily, value]) => {
    return (
      <Box key={`${fontFamily}`}>
        <Text fontFamily={fontFamily}>
          fontFamily: {fontFamily}
        </Text>
        <Text fontFamily={fontFamily}>
          value: {value}
        </Text>
        <Box>
          {Object.entries(fontWeights).map(([fontWeight, value]) => {
            return (
              <Box key={`${fontWeight}`} className="flex-direction--row">
                <Text fontFamily={fontFamily} fontWeight={fontWeight}>
                  fontWeight: {fontWeight}
                </Text>
                <Text fontFamily={fontFamily} fontWeight={fontWeight}>
                  value: {value}
                </Text>
              </Box>
            )
          })}
        </Box>
      </Box>
    )
  })}
</Box>

</Box>
</Unstyled>
