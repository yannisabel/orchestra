import { Canvas, Meta, Story } from '@storybook/blocks'
import { Card } from '../../Staves/Card'
import { Box } from '../../Staves/Box'
import { Text } from '../../Staves/Text'

import { colors } from '.'

<Meta title="Notations/Colors" />

# Colors

<Box className="flex-direction--column">
  {Object.entries(colors).map(([colorName, value]) => {
    return (
      <Card key={`${colorName}`} className="display--flex align-items--center">
        <Box style={{backgroundColor: value, width: '50px', height: '50px'}} />
        <Box className="flex-direction--column">
          <Text fontFamily="ff-openSans">
            colorName: {colorName}
          </Text>
          <Text fontFamily="ff-openSans">
            value: {value}
          </Text>
        </Box>
      </Card>
    )
  })}
</Box>
