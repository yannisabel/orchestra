import { Canvas, Meta, Story } from '@storybook/addon-docs'

import { Box } from '../layout'
import { Text } from '../text'

import {colors} from '.'

<Meta title="Instruments/Colors" />

# Colors

<Canvas>
  <Story name="Default">
    {Object.entries(colors).map(([colorName, value]) => {
      return (
        <Box backgroundColor={colorName} key={`${colorName}`}>
          <Text color={colorName.startsWith('black-') || !colorName.startsWith('white-') || colorName.startsWith('grey-') ? 'white-0' : 'grey-100'}>
            colorName: {colorName}
          </Text>
          <Text color={colorName.startsWith('black-') || !colorName.startsWith('white-') || colorName.startsWith('grey-') ? 'white-0' : 'grey-100'}>
            value: {value}
          </Text>
        </Box>
      )
    })}
  </Story>
</Canvas>
