import { Canvas, Meta, Story } from '@storybook/addon-docs'

import { Button } from './Button'

<Meta title="Symbols/Button" component={Button} />

export const Template = (args) => <Button {...args} />
export const TemplateWithBackground = (args) => <Button {...args} />

# Button

<Canvas>
  <Story
    name="Default"
    args={{
      children: 'Button',
      model: 'default',
      onClick: () => console.log('coucou')
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Round"
    args={{
      model: 'round',
      icon: 'image',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Ghost default"
    args={{
      children: 'Button',
      model: 'default',
      state: 'ghost',
      onClick: () => console.log('coucou')
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Ghost Round"
    args={{
      model: 'round',
      state: 'ghost',
      icon: 'image',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Raised"
    args={{
      children: 'Button',
      model: 'default',
      state: 'raised',
      color: 'blue',
      onClick: () => console.log('coucou')
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable of={Button} />
