import { Canvas, Meta, Story } from '@storybook/addon-docs'

import { colors } from '.'

<Meta title="Instruments/Colors" />

# Colors

<Canvas>
  <Story name="Default">
    <Box className="flex-direction--column">
      {Object.entries(colors).map(([colorName, value]) => {
        return (
          <Box key={`${colorName}`}>
            <Box style={{backgroundColor: value, width: '50px', height: '50px'}} />
            <Box className="flex-direction--column">
              <Text className="ff-openSans">
                colorName: {colorName}
              </Text>
              <Text className="ff-openSans">
                value: {value}
              </Text>
            </Box>
          </Box>
        )
      })}
    </Box>
  </Story>
</Canvas>
