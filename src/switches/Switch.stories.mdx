import { useState } from 'react'
import { Canvas, Meta, Story } from '@storybook/addon-docs'

import { Switch } from './Switch'

<Meta
  title="Symbols/Switch"
  component={Switch}
  argsType={{
    label: 'label',
    optionTrue: 'true',
    optionFalse: 'false',
    ariaChecked: false,
    onAction: () => {alert('Switched')}
  }}
/>

# Switch

<Canvas>
  <Story
    name="Default">
    {() => {
      const [option, setOption] = useState(false)
      return <Switch label="label" optionTrue="true" optionFalse="false" onAction={() => setOption(!option)} ariaChecked={option} />
    }}
  </Story>
</Canvas>
