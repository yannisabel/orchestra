import { Canvas, Meta, Story } from '@storybook/addon-docs'

import { Box } from '../Box'
import { Title } from './Title'

<Meta title="Staves/Title" component={Title} />

# Title

## Default

<Canvas>
    <Story
      name="Default"
    >
        { 
          ['h1', 'h2', 'h3', 'h4', 'h5', 'h6'].map(function(title, i){
            return (
              <Title key={title} type={title}>An {title} title</Title>
            )
          })
        }
    </Story>
</Canvas>

## Props

<ArgsTable of={Title} />
