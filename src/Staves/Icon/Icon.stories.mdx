import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs'

import { Icon } from './Icon'
import { Text } from '../Text'
import { Stack } from '../Stack'
import { Box } from '../Box'

import { allIcons } from './Icon.types'

<Meta title="Staves/Icon" component={Icon} />

# Icon

<Canvas>
  <Story name="Default">
    {allIcons.map((IconName) => {
    return (
      <Stack key={IconName} direction="column" spacing="space-1" className="align-items--center" style={{width: '100px', height: '100px'}}>
        <Box style={{width: '50px', height: '50px'}}>
          <Icon name={IconName} className="icon-c--textColor" />
        </Box>
        <Text>{IconName}</Text>
      </Stack>
    )
  })}
  </Story>
</Canvas>

## Props

<ArgsTable of={Icon} />
