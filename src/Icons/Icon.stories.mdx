import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs'

import { Icon } from './Icons'
import { Text } from '../Text'
import { Stack } from '../Stack'
import { Box } from '../Box'

import { allIcons } from './Icon.types'

<Meta title="Symbols/Icons" component={Icon} />

# Icons

<Canvas>
  <Story name="Default">
    {allIcons.map((IconName) => {
    return (
      <Stack key={IconName} className="stack--column column-space-1 align-items--center" style={{width: '100px', height: '100px'}}>
        <Box style={{width: '50px', height: '50px'}}>
          <Icon name={IconName} className="icon-c--textColor" />
        </Box>
        <Text>{IconName}</Text>
      </Stack>
    )
  })}
  </Story>
</Canvas>

## Props

<ArgsTable of={Icon} />
