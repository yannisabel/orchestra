import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs'

import { Icon } from './Icons'

import { allIcons } from './Icon.types'

<Meta title="Symbols/Icons" component={Icon} />

# Icons

<Canvas>
  <Story name="Default">
    {allIcons.map((IconName) => {
    return (
      <Icon key={IconName} name={IconName} size={30} />
    )
  })}
  </Story>
</Canvas>

## Props

<ArgsTable of={Icon} />
